export { featureFlags, isFeatureEnabled } from "@/lib/feature-flags";

/** Placeholder — wire Resume Enhancer module here when featureFlags.resumeModule is true. */
export function ResumeModulePlaceholder() {
  return null;
}
