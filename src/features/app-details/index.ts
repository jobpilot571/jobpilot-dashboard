export { featureFlags, isFeatureEnabled } from "@/lib/feature-flags";

/** Placeholder — Application Details + token form when featureFlags.appDetailsModule is true. */
export function AppDetailsModulePlaceholder() {
  return null;
}
