export { featureFlags, isFeatureEnabled } from "@/lib/feature-flags";

/** Placeholder — Trial chat when featureFlags.trialChatModule is true. */
export function TrialChatModulePlaceholder() {
  return null;
}
